orb_sdk_path = '../orb-sdk/module'
load File.join(orb_sdk_path, '.ios', 'Flutter', 'podhelper.rb')

platform :ios, '9.0'

target 'OrbDemo' do
    use_frameworks!
    install_all_flutter_pods(orb_sdk_path)
end
