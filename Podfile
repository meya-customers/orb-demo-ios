platform :ios, '9.0'

target 'OrbDemo' do
    use_frameworks!
    # Use OrbDebug to run the Orb SDK using the iOS Simulator
    pod 'OrbDebug', '2.7.4', :source => 'https://github.com/meya-customers/orb-cocoapods'
    
    # Use Orb for release and archive builds - this includes bitcode for Orb & Flutter
    # pod 'Orb', '2.7.4', :source => 'https://github.com/meya-customers/orb-cocoapods'
end
